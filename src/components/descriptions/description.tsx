import React from 'react';
import { Container, Info } from './styles';

const Description = () => {
  return (
    <Container>
      <Info>Yarn Workspaces Monorepo support for Create-React-App / React-Scripts.</Info>
    </Container>
  );
}

export default Description
